save all unique hits that can be analyzed later.

- Implement keyboard shortcuts properly (currently registered but not working correctly)
  - Ensure useKeyboardShortcuts hook is called with proper handler functions
  - Remove duplicate event listeners that may conflict
  - Test all shortcuts (S, H, C, L, M, ?, Arrow Up/Down, Esc) in all views
  - Add shortcut hints in UI for discoverability